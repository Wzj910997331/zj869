# GLM 复核与修正报告（2026-08-31）

> 本机 Linux 复跑 Windows 端 `verify_stragglers --model glm-5.3-flash`，并完成 `image_patterns_with_blogger.json` 修正。
> 模型：**glm-5.3-flash**（网关 llm.riverbegin.cn）；直接读本地原图 `data/crawl/260828/` 与 `data/crawl/20260828/images/`。
> 26230 实际开奖：**9 4 6 8 3**。
> **位置判定依据**：GLM 对每张图先校准 26229 = 2 8 0 5 4，列定位通过校准行验证；deepseek 原读数的位置存在系统性列偏移（约 1 列，疑似把列/和值列算进位置）。

## 修正动作汇总

- **修改 13 条**、**剔除 4 条**，总记录 704 → 700。
- 明细：`data/crawl/20260828/glm_corrections_manifest.json`（含 before/after 与删除记录原文，可回溯、可在 Windows 源上同步同样修改）。
- 被改记录统一加 `"verified_by": "glm-5.3-flash 2026-08-31"` 标记。

## 三处待定夺案例 → 已按 GLM 校准行证据定案

### 1. 微时光_1 → REJECT 定案（无需再人工看图）
- 图 `data/crawl/260828/微时光/s_2_0f08b4cd-f29d-44da-86b7-fed91bd56ce4_1.jpg`
- GLM：校准 26229 ✓，26230 预测 = **万位6（青圈）、十位3（红圈）**。三路互证（deepseek 复核×12 + Windows-GLM + 本机-GLM）。
- 修正：原记录「百位6 hit=True / 个位3 hit=True」是 deepseek 列偏移的**假命中**（撞中 26230 百位=6、个位=3），已改为 **万位6 hit=False、十位3 hit=False**。
- 结论：预测实际全部未中 → **REJECT 成立**，可正式定案。

### 2. 乐仔红格 → 红格=预测候选，非杀号；位置按校准行法修正（请顺带瞄一眼图）
- 图：`data/crawl/260828/乐仔👑1288/` 及 `data/crawl/20260828/images/s_2_9e2aa187-*.jpg`（共 6 张，GLM 全核）
- GLM（逐格，列边界验证）：**所有红格均为预测候选**（实心红底白字、线连入格内、无 X、无"杀"字）。
  - `_0`：万位"49"、千位"05"
  - `_1`/`_2`：百位"49"、十位"38"
  - `_3`/`_5`：万位"49"、十位"49"
  - `_4`：十位"49"
- **纠正此前误判**：deepseek 的"个位38"不是幻觉，而是**位置读偏**——38 实际在**十位**（`_1`/`_2`），不是个位。此前报告"38 不存在"作废。
- 修正：12 条 `杀号` 红格记录 → `定位`（预测），位置与命中全部重算（见 manifest）。
  - 例：`_0` 万位49 → 定位 万位 hit=True（26230 万位=9）；`_1` 十位38 → 定位 十位 hit=True（十位=8）；`_4` 胆码49 → 定位 十位 hit=False。
- 若按旧"杀号"口径，乐仔 3 图杀号大多失败；按新"预测"口径，乐仔万位/十位预测命中约 6/11。**语义差异决定博主命中统计，请最终以图为准。**

### 3. 辉拓数据_4 → X 打在空格、无被杀数字 → 剔除（请你确认）
- 图 `data/crawl/260828/辉拓数据/s_2_040f42e9-..._4.png`（记录文件名为 `_4.jpg`，同图）
- GLM（放大裁片逐格）：26230 行仅第2位(千)、第4位(十)各有紫色 X（另一轮读 `_2.png` 为第1/第3位，两图结构不同）；**X 均打在空格上，块内无数字**，无"杀"字。
- 修正：`_4` 与同缺陷的 `_2` 共 **4 条空号杀号记录全部剔除**（`nums=[]`，原 hit=True 是空 `all()` 的假象，无法作为杀号规则）。
- 建议：辉拓 X 标记语义不明确（链式末端标记），若后续要纳入需人工标注被杀对象。

## 系统性发现（重要）

- deepseek 在完整图上的**位置读数存在系统性列偏移**，已确认影响 微时光、乐仔；可能波及更多博主的定位/胆码记录。
- 影响：M2 规律池 / M3 决策的位置口径可能整体有偏；旧「图片规律总结」报告中的位置命中统计不可直接采信。
- **建议下一步**：用 glm-5.3-flash 对全部 ~219 张规律图做一次全量复核（校准行法），重建 `vision_patterns_full.json` 级的位置可信数据，再重跑 M2/M3。本机网关已可用，可随时执行（约 219 次 GLM 读图，成本请知悉）。

## 产物

- `data/crawl/20260828/image_patterns_with_blogger.json` — 已修正（700 条）
- `data/crawl/20260828/glm_corrections_manifest.json` — before/after 明细
- `data/crawl/20260828/verify_results_glm_local.json` — 复核结论
- `docs/GLM复核报告-20260831.md` — 本报告
