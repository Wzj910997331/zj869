# 命中规律库 — 26231 期

> 期号：**26231** ｜ 开奖：**1 8 7 9 9**（万 千 百 十 个） ｜ 口径：博主目标期行手写+单押
> 采集记录：**80 条** ｜ 命中：**4 条（5.00%）** ｜ 完全命中（1位置1中）：4 条
> 规律条数：**4 条**

| 博主 | 命中位置 | 全图预测明细（各位置对错） | 博主画规逻辑 |
|---|---|---|---|
| 用规说话 | 定位 万位=1 → 26231 万位=1 ✓ | 万1✓(实际1) | 博主在目标期行手写万位=1 |
| 富老师 | 定位 万位=1 → 26231 万位=1 ✓ | 万1✓(实际1) | 博主在目标期行手写万位=1 |
| 生活很无奈 | 定位 万位=1 → 26231 万位=1 ✓ | 万1✓(实际1)；千9✗(实际8)；百0✗(实际7)；十0✗(实际9)；个5✗(实际9) | 博主在目标期行手写万位=1 |
| 487199630 | 定位 十位=9 → 26231 十位=9 ✓ | 万8✗(实际1)；十9✓(实际9) | 博主在目标期行手写十位=9 |

## 位置校验说明（2026-09-02）

位置名**绝不盲信 GLM 读数自带的位置名**（那是循环校验：模型说"百位"就查百位）。命中位置一律经**校准行锚定**：
用前一期已知开奖 **26230 = 9 4 6 8 3** 在图上的印刷数字列位，反推出万千百十个真实列位，再定位博主手写数字所在列。

- **富老师（图 _2）已改判为 MISS（单码错位）**：GLM 原读"百位=7"（且百位实开7，故误判命中）。校准行锚定后"7"实为**千位=7**，千位实开 8 → **未中**。**旧报的"百位7 命中"是期号/和值列污染 filter_report.cols 造成的列位错移假命中。**
- **富老师（图 _0）万位=1 命中**：红圈"1"正压在 26230 万位(9)正下方，箭头从万9直插目标行 → 万位=1，实开万=1 ✓。（同一博主 _0 命中、_2 错位，勿混。）
- **487199630 万位=8 未中 / 十位=9 命中**：校准行 26230=9 4 6 8 3，博主圈 万9+十8 画交叉线；目标行"9"压在十位(8)正下方 → 十位=9，实开十=9 ✓。
- **生活很无奈 万位=1 命中**：窄条五列齐全 `1 9 0 0 5`，校准列 9,4,6,8 全对位 → 万位=1 ✓。
- **用规说话 万位=1 命中**：红圈"1"正压在 26230 万位(9)正下方，箭头自万9直插 → 万位=1 ✓。

> 校验已固化进 `verify_blogger_prediction.py --position-overrides`（逐条记 `position_source`：calib-anchor / glm-read）。

## 被剔除的命中（本期修正）

- **洛河图算**：多码宽网(≥2 候选)：候选池式，推不出本期唯一结果（万位 候选[0, 5] 实际1）
- **突如钱来**：多码宽网(≥2 候选)：候选池式，推不出本期唯一结果（万位 候选[3, 6] 实际1）
- **袁爽:五花肉**：多码宽网(≥2 候选)：候选池式，推不出本期唯一结果（千位 候选[4, 9] 实际8）
- **用规说话**：多码宽网(≥2 候选)：候选池式，推不出本期唯一结果（万位 候选[1, 6] 实际1）
- **487114836**：多码宽网(≥2 候选)：候选池式，推不出本期唯一结果（万位 候选[0, 3, 5] 实际1）
- **_逆境崛起_2316181**：多码宽网(≥2 候选)：候选池式，推不出本期唯一结果（万位 候选[3, 8] 实际1）
- **易尚**：未读出博主目标期行预测（无 predicted_positions）
- **打奖好穷**：未读出博主目标期行预测（无 predicted_positions）
- **五行秘法**：未读出博主目标期行预测（无 predicted_positions）
- **小目标ww**：报号/无画规：和值/组选/胆(非定位单码)：和值16
- **信可乐也**：未读出博主目标期行预测（无 predicted_positions）
- **富老师**：未读出博主目标期行预测（无 predicted_positions）
- **一开张吃三年**：未读出博主目标期行预测（无 predicted_positions）
- **9394948**：未读出博主目标期行预测（无 predicted_positions）
- **坦荡荡！**：未读出博主目标期行预测（无 predicted_positions）
- **李日朗Don**：未读出博主目标期行预测（无 predicted_positions）
- **难得糊涂_483396764**：未读出博主目标期行预测（无 predicted_positions）
- **一七八**：未读出博主目标期行预测（无 predicted_positions）
- **范有疯癫_483563145**：未读出博主目标期行预测（无 predicted_positions）
- **关心❤️**：未读出博主目标期行预测（无 predicted_positions）
- **486855127**：报号/无画规：和值/组选/胆(非定位单码)：和值8
- **吴轲wolf**：未读出博主目标期行预测（无 predicted_positions）
- **微时光**：读图/api 失败：glm-5.3-flash 输出非 JSON 数组

> 规律为博主手绘画规的历史总结，彩票开奖属独立随机事件，不具备预测效力。